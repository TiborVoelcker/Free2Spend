"""Sources of transactions, normalised into the engine's shapes.

Each bank export format gets its own module. The demo generator (`demo/`) is
the same kind of thing from the engine's point of view: something that produces
`Transaction` objects.
"""
